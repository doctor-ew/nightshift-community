# Test evidence assessment — issue 35

You are the Codex test role, read-only. Do not write files, start role/factory workers, download models, or run tests requiring temporary writes in your read-only sandbox. The controller executes the selected commands under existing user authorization and supplies actual exit statuses and immutable log hashes in docs/35/test-execution.json. Read the context resolver output, AGENTS.md and CLAUDE.md first, as required by your role. Both are silent on an explicit suite; the sealed spec defines bash tests/test-ticket-accounting.sh and the controller records the relevant regression commands with source attribution.

Assess the actual supplied commands, exit statuses and full logs, confirm assertion counts and failures, and return results.passed/results.failed plus schema fields. SUCCESS requires actual passing evidence and failed=0; never treat SKIP or an absent report as green. Explicitly say tests were executed by the write-capable controller and you inspected the evidence; do not claim you executed them. Return any report text in artifacts.diff for controller integration, not a file write. No implementation diagnosis or source repair.
