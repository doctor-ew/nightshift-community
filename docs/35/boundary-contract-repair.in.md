# Integration contract repair, controller source review
Approved plan; read-only component patch proposal. Return apply_patch-formatted artifacts.diff. No tests or logs or proof reads, no writes, no nested workers. Controller applies and validates. Allowed scripts/nightshift-run-metrics.py, scripts/nightshift-agent.sh, scripts/nightshift-factory.sh, scripts/nightshift-ticket-source.sh, scripts/nightshift-provider-usage.py. Focus only the concrete production-boundary defects below.

1. Provider parser emits numeric stream_epoch, but metrics _sanitize_receipt only permits string; actual boundary receipts are rejected. Normalize nonnegative integer epochs into stable canonical identity strings while preserving existing string epochs.
2. _pricing_sources drops all parser estimates because parser emits runtime_version and normalizer_version keys. Align typed allowlisting with actual provider metadata, retain numeric normalizer version and explicit unknown runtime version. Never persist arbitrary payloads.
3. Factory interruption handler captures only legacy event and summary, not already captured provider receipts. Persist available captured observations with interrupted status before final summary; expected unmeasured calls remain explicit. Ensure capture reader cannot race unfinished tee output on normal completion. Observational capture failure must never change provider exit behavior.
4. Ticket binding is incomplete: factory --derive-id gh:35 returns null repository; dispatcher bare --task35 cannot derive any ticket unless inherited single-ticket env. Resolve short GitHub repo from authoritative configured origin URL or verified fetched ticket canonical URL, never cwd. For standalone/batch task binding, use matching already-fetched docs/<task>/ticket.json with canonical source/id/repository/url verification, or verified prepared ticket context. Preserve unknown attribution when unavailable. No upstream fetching or guessed ticket names; no auth/routing changes.

These are production source findings, not hidden test feedback. Read only necessary functions and public SPEC-DIGEST, propose the smallest complete repair.
