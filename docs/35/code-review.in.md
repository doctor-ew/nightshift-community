# Independent post-implementation review, issue 35

You are the independent Claude reviewer of Codex-authored specification, implementation proposals and controller-integrated edits. Read-only; do not modify files or launch role/factory workers. Review current working files, including uncommitted changes and the new parser, against docs/35/SPEC-DIGEST.md. Use base 92b5d71895cc43c97c3effad3a43826ce3f74056, not HEAD-only diff. Read actual changed source fully, plus targeted callers as needed. No need to re-fetch primary provider docs: retained approved citations and source snapshots establish provider semantics.

Review seven implementation paths declared in docs/35/implementation.in.md, and tests/test-ticket-accounting.sh plus tests/test-run-metrics.sh for coverage. Review six lenses: DRY, SOLID, ACID, CoC, Big O and LLM trust boundary, plus all eight acceptance criteria and spec/diff alignment. Check actual wiring, not only helper unit tests. Pay particular attention to trusted ticket identity, real provider usage on error/interruption, immutable replay/conflicts, overlapping SDK/external children, unknown vs zero, category totals and cost provenance, private allowlisting, nonfatal filesystem/subprocess boundaries and legacy compatibility. Routing/authentication/engineering gates must remain unchanged.

Return schema-valid claims: each verified criterion/lens is VERIFIED with actual file/line evidence; each HIGH/MEDIUM blocking issue is CONFLICT and claim text includes lens, severity BLOCK, confidence, concrete trigger and impact. Low-confidence findings are NOTE, actionable nonblocking issues WARN. Explicitly report six lens verdicts. SUCCESS only if no blocking findings; FAIL otherwise. artifacts.diff may contain a concise Markdown review, never code edits. No self-approval: author provider is Codex and your dispatcher provider must be Claude.
