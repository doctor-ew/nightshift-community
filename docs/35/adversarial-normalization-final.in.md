Read-only independent review of final normalization clarification. No workers, writes, or shell/network calls. Earlier report verified total=input+output and reasoning non-additive but conflated the spec-defined fresh_input partition with Codex non_cached_input(). The spec now explicitly separates them.
Read docs/35/SPEC.md token paragraph, docs/35/codex-response-usage.txt lines 127-166 and 905-936, and docs/35/codex-token-semantics.txt lines 2415-2428.
Evaluate two ordered claims:
1. [EXISTING] Codex non_cached_input() = input minus cache reads, hence its value includes cache writes; overall provider total input+output does not add cache/reasoning details.
2. [NEW] Nightshift defines a DIFFERENT normalized fresh_input category, residual input minus reads minus writes, to keep the four reported categories disjoint. For the upstream fixture input100/read40/write60/output10/reasoning5/total110, Nightshift normalized fresh0/read40/write60/output10 totals110, while Codex non_cached_input is60. This is a new normalization convention, not a claim about an existing Codex method. Invalid or missing category observations remain incomplete rather than guessed. Verify this proposed contract is consistent with the demonstrated source values; flag a genuine inconsistency, not the expected absence of new implementation.
Return complete claim records. This is the final bounded external normalization repair; do not invent approval if a real unresolved contradiction remains.
