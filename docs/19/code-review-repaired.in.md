Independently re-review issue19 after a concrete defect was found despite the prior static approval. Read docs/19/SPEC.md, docs/19/receipts/code-review-resolution.md, scripts/nightshift-branding.py, scripts/nightshift-install-inventory.py, and the added sealed regression in tests/test-branding.sh at bot commit6343bc4. The actual bug: raw symlink target contains a symlink directory followed by '..'; lexical collapse matched expected source, but actual OS resolution differed, so installed-only audit read a different file and falsely passed. New implementation preserves component semantics and rejects escapes before target metadata/content access. Verify safe_source and mapped_link_source in both installed leaf and tree traversal, ordinary leading-relative parent links, loops, internal links, wrong targets, and exact-source ownership. Verify no remaining contradictions with the earlier static review; inspect current changed files as needed, not just textual existence. Author codex; reviewer must be independent. No edits, provider launches, installation, or Coach changes. State tests not executed if CLI permission blocks them; dynamic tests run independently outside this reviewer. Return strict extractor evidence claims and approve/request-changes rationale. Do not treat provider return status as code approval.
