# Specification review resolution

The first independent review requested changes. Its substantive attempt remains charged in the retained budget.

- Preserve the existing subprocess-authorization documentation. The amended specification proposes a separate branding-audit document.
- Include the explicit legacy Python resolver destination and installed project-context documentation in the shared inventory.
- Place the audit exit immediately after argument validation and destination calculation, before the existing conflict-section directory creation. Remove the obsolete late check branch. An earlier exit prevents that existing write; moving the conflict operation elsewhere is unnecessary.
- Specify exact policy keys, rule identifiers, size ceilings, ownership tree derivation, and source citations before sealing.

Implementation remains gated on the amended independent review and RED seal.

The amended cross-provider review recommends approval conditional on naming the preflight environment clear. The exact operation clears the legacy variable rather than resolving it. The specification now names this compatibility context. Independent reviewer review_18 checked the condition against the actual operation and recommends sealing, with the test hash unchanged. The second batch is accepted after this explicit condition is satisfied; the first substantive failure remains retained.
