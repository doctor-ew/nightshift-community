# Issue 13 validation

## Completed checks

The independent behavioral proof suite passed all 25 cases after two independently reviewed fixture amendments. The first clarified the forbidden-description string and adapted the transport stub to the verified reviewer invocation. The second sealed a changed oracle before initial challenge and proof sealing. Neither amendment changed the behavioral assertions. The original failing runs and review receipts remain retained.

The dispatcher suite passed 95 assertions, including installed copy and symlink execution. The initial symlink failure was repaired by hashing the resolved engine source; repository and evidence symlink rejection remain enforced. The metrics suite passed with the common-Git filesystem access required by its factory fixtures. The sandbox failure is retained separately and is not described as a metrics logic failure.

The full offline harness passed 35 suites with zero failures. The factory adversarial trajectory passed through ordinary reviewed documentation admission. TDD integrity passed with the original recovery seals and separately reviewed bot amendments preserved.

Isolated copy and symlink installations passed their source/installed audits: 310 source artifacts and 162 installed artifacts, zero findings. The legacy adapter completed ordinary deterministic development and final proof with its optional metrics helper absent. The active user installation was not updated.

## Real execution

The manual bootstrap evidence is recorded in BOOTSTRAP.md. It is separate from execution through the completed engine.

The completed-engine source fixture passed its first independent design challenge. Its first development attempt stopped during the version probe, before a prototype launch: the reviewed fixture named CLI 2.1.265 while the executable reported 2.1.266. The canonical ledger retained one design launch and one infrastructure observation. The runtime-only amendment was independently reviewed; public/private case bodies, oracle values and IDs were unchanged. The same project and ledger are resumed, without resetting counters.

The pinned external executable reports 2.1.266 (Claude Code), SHA256 553d1b9e9e7068b275c0a783c7e139ff6503096f286e674c8c919379fb0eca62. Its local help exposes the required safe-mode, tool suppression, session suppression and text-output controls. Source and copied-runtime executions each passed two development and two held-out final cases.

The symlink runtime initially stopped before model launch because the routing asset reader rejected its legitimate installation symlink. The repair resolves the trusted routing asset before reading and hashing it; strict proof/evidence/source artifact link checks remain. The same symlink project resumed with its failure and counters retained, and passed two development and two held-out final cases. This real execution binds engine SHA256 3cdd53a30b5dab496a672d07aa9d3c9222bef81a2f00c5ac471842159ac00299. The independent 25-case suite passed again after this repair.

Source/copy passes bind the earlier engine revision. They are retained historical results, not fresh gates for a different helper hash. The final symlink acceptance executes the repaired source engine. No exhausted final budget was reset or resampled to manufacture current evidence. All three runs use the same independently fixed cases to compare runtime packaging; they are not three independently sampled held-out datasets.

## Retained logs

Logs are retained under /private/tmp with these names: nightshift-13-green-proof-attempt-1.log, nightshift-13-green-proof-attempt-2.log, nightshift-13-green-proof-attempt-3.log, nightshift-13-green-dispatch-attempt-1.log, nightshift-13-green-dispatch-attempt-2.log, nightshift-13-green-metrics-attempt-1.log, nightshift-13-green-metrics-attempt-1-escalated.log, nightshift-13-green-evals-attempt-1.log, nightshift-13-trajectory-attempt-1.log, nightshift-13-trajectory-attempt-2.log, nightshift-13-install-copy-check.log, nightshift-13-install-symlink-check.log and nightshift-13-legacy-proof.log. Private execution artifacts remain outside the delivery checkout.

## Outstanding delivery gates

Independent code review and drift assessment approve the implementation. Hosted CI passed at 5d9f457, including ShellCheck, dashboard validation and offline/behavioral regressions. The final delivery-documentation commit must pass CI before integration merge. This receipt does not claim that merge has occurred.
